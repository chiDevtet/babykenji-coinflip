# Wallet and Account Matrix

Use placeholders until real addresses are selected. Do **not** paste private keys into docs, frontend env, logs, or PRs.

| Name | Type | Purpose | Owner/controller | Secret key required? | Custody tier | Needs SOL? | Needs Baby Kenji? | Env var | Used by | Devnet placeholder | Mainnet placeholder | Security notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Program ID | Program account | Deployed Anchor program id | Upgrade authority controls upgrades | No | N/A | Funded through deploy | No | `PROGRAM_ID`, `VITE_PROGRAM_ID` | deploy, backend, frontend | `<PROGRAM_ID>` | `<PROGRAM_ID>` | Must match `declare_id!`, Anchor.toml, backend, frontend. Current repo contains a real configured id in `program/Anchor.toml`; replace only through normal release process. |
| Program deployer keypair | EOA/keypair | Pays deploy transaction costs | Release engineer | Yes | Warm/cold | Yes, significant for deploy | No | `ANCHOR_PROVIDER_WALLET` / Solana config wallet | on-chain deploy | `./keys/deployer.json` | hardware/multisig-controlled deploy signer | Never run backend with deployer key. |
| Program upgrade authority | EOA/keypair or multisig | Can upgrade program | Protocol governance | Yes if EOA | Cold/multisig | Yes for upgrade tx fees | No | Solana program authority, not app env | admin/operator | `<UPGRADE_AUTHORITY>` | `<UPGRADE_AUTHORITY_MULTISIG>` | Mainnet should be multisig/cold or risk explicitly accepted. |
| Anchor provider wallet | EOA/keypair | Anchor build/deploy/test signer | Release engineer | Yes | Warm | Yes | Maybe if also admin deposits | `ANCHOR_PROVIDER_WALLET` | deploy/tests/scripts | `<ANCHOR_PROVIDER_WALLET>` | `<ANCHOR_PROVIDER_WALLET>` | Usually same as deployer on devnet only. |
| Admin wallet | EOA/keypair or multisig | Initializes config, deposits/withdraws vaults, updates config, pause/unpause | Protocol admin | Yes | Mainnet multisig/cold | Yes for tx fees and SOL bankroll deposits | Yes if funding token vault | `ADMIN_WALLET` / script wallet | config init, admin/operator | `<ADMIN_WALLET>` | `<ADMIN_WALLET_MULTISIG>` | Program stores this in `GameConfig.admin`. |
| Pending admin wallet | EOA/keypair or multisig | Accepts admin transfer | New admin | Yes | Cold/multisig | Yes for accept tx | No | `PENDING_ADMIN_WALLET` | admin/operator | `<PENDING_ADMIN_WALLET>` | `<PENDING_ADMIN_WALLET_MULTISIG>` | Set by `propose_admin_transfer`; not active until accepted. |
| GameConfig PDA | PDA | Global config for one Baby Kenji mint | Program-derived from `["config", mint]` | No | Program | Rent paid by admin at init | No | Derived from `PROGRAM_ID` + `TOKEN_MINT` | all | derived | derived | Stores admin, settle authority, mint, vaults, fees, payouts, limits, liabilities. |
| Token treasury vault | PDA-owned SPL token account | Baby Kenji bankroll for token flips | Token authority is GameConfig PDA | No | Program | Rent at init | Yes; bankroll deposits | derived | program, backend, frontend | derived | derived | Seed `["vault", config]`; receives wagers and pays token wins. |
| SOL vault | PDA account | Native SOL bankroll for SOL flips | Program-owned PDA | No | Program | Yes; rent + bankroll | No | derived | program, backend, frontend | derived | derived | Seed `["sol_vault", config]`; receives SOL wagers and pays SOL wins. |
| PlayerState PDA | PDA | Per-player nonce | Program-derived `["player", config, player]` | No | Program | Player pays creation rent | No | derived | frontend/program | derived | derived | Nonce controls Bet PDA. |
| Bet PDA | PDA | Per-wager commitment and settlement state | Program-derived `["bet", player, nonce]` | No | Program | Player pays rent; closed to player | No | derived | backend/program/frontend | derived | derived | Current layout changed; old accounts need migration/reinit. |
| Switchboard program ID | Switchboard account/program | Owner of randomness accounts | Switchboard | No | External | No | No | `SWITCHBOARD_PROGRAM_ID`, `VITE_SWITCHBOARD_PROGRAM_ID` | backend/frontend/program verification | `<SWITCHBOARD_PROGRAM_ID>` | `<SWITCHBOARD_PROGRAM_ID>` | Default build expects devnet owner; `mainnet` feature expects mainnet owner. |
| Switchboard queue | Switchboard account | Queue used to create/commit randomness | Switchboard/operator-selected | No | External | No | No | `SWITCHBOARD_QUEUE`, `VITE_SWITCHBOARD_QUEUE` | config/init/frontend/backend | `<SWITCHBOARD_QUEUE>` | `<SWITCHBOARD_QUEUE>` | Must be correct cluster; never use devnet queue on mainnet. |
| Switchboard randomness account | Switchboard account/keypair | Per-bet randomness account | Frontend/player creates random keypair | Ephemeral secret needed to create account | Hot ephemeral client | Player pays tx/rent | No | none global | frontend/program/backend settle | generated per bet | generated per bet | Program stores address in Bet and checks owner/seed slot. |
| Randomness authority | EOA/keypair | Param stored in config for future/randomness authority semantics | Operator/backend if used | Yes if used | Hot/warm | Yes if it signs txs | No | `RANDOMNESS_AUTHORITY` | init/admin | `<RANDOMNESS_AUTHORITY>` | `<RANDOMNESS_AUTHORITY>` | Current code stores it but does not enforce it in place/settle. |
| Settle authority / backend fee payer | EOA/keypair | Backend signs settlement, reveal, refund, seed rotation txs; pays fees | Backend operator | Yes | Hot with limited SOL | Yes, limited tx-fee SOL | No | `SETTLE_AUTHORITY_KEYPAIR_PATH` | backend | `./keys/settle-authority.json` | `/run/secrets/settle-authority.json` | Do not overfund; rotate if compromised. |
| Team SOL wallet | EOA | Intended SOL fee recipient | Team treasury | No to receive | Cold/multisig | No, only receives | No | `SOL_TEAM_WALLET` / legacy `TEAM_FEE_WALLET` | docs/scripts/future fee plumbing | `<TEAM_SOL_WALLET>` | `<TEAM_SOL_WALLET>` | Current settlement routes holder rewards to this configured wallet/account; automatic distribution remains disabled unless HOLDER_REWARDS_MODE=automatic is separately tested. |
| Dev/buyback SOL wallet | EOA | Intended SOL dev/buyback fee recipient | Dev/buyback treasury | No to receive | Cold/multisig | No, only receives | No | `SOL_DEV_BUYBACK_WALLET` / legacy `DEV_FEE_WALLET` | docs/scripts/future fee plumbing | `<DEV_BUYBACK_SOL_WALLET>` | `<DEV_BUYBACK_SOL_WALLET>` | Current settlement routes holder rewards to this configured wallet/account; automatic distribution remains disabled unless HOLDER_REWARDS_MODE=automatic is separately tested. |
| Holder rewards SOL wallet | EOA | Intended SOL holder reward accumulation/source | Rewards operator/multisig | Yes if distributing SOL | Warm/multisig | Yes if source and paying fees | No | `HOLDER_REWARDS_SOL_WALLET` | rewards worker | `<HOLDER_REWARDS_SOL_WALLET>` | `<HOLDER_REWARDS_SOL_WALLET>` | Current settlement routes holder rewards to this configured wallet/account; automatic distribution remains disabled unless HOLDER_REWARDS_MODE=automatic is separately tested. |
| Baby Kenji mint | SPL mint | Wager token mint | Token mint authority/project | No for app | Cold | No | Mint supply only | `TOKEN_MINT`, `BABY_KENJI_MINT`, `VITE_TOKEN_MINT` | all | `<BABY_KENJI_MINT>` | `<BABY_KENJI_MINT>` | Must match config PDA seed and all token accounts. |
| Team Baby Kenji fee account | SPL token account/ATA | Intended token fee receiver | Team treasury | No to receive | Cold/multisig owner | Rent if created | Receives fees | `TOKEN_TEAM_FEE_ACCOUNT` | future fee plumbing | `<TOKEN_TEAM_FEE_ACCOUNT>` | `<TOKEN_TEAM_FEE_ACCOUNT>` | Must have Baby Kenji mint. Not currently included in settle context. |
| Dev Baby Kenji fee account | SPL token account/ATA | Intended token dev fee receiver | Dev/buyback treasury | No to receive | Cold/multisig owner | Rent if created | Receives fees | `TOKEN_DEV_FEE_ACCOUNT` | future fee plumbing | `<TOKEN_DEV_FEE_ACCOUNT>` | `<TOKEN_DEV_FEE_ACCOUNT>` | Must have Baby Kenji mint. Not currently included in settle context. |
| Holder rewards Baby Kenji account | SPL token account/ATA | Intended token rewards accumulation/source | Rewards authority/multisig | Yes for distribution authority | Warm/multisig | SOL for token tx fees/ATAs | Receives fees/source tokens | `TOKEN_HOLDER_REWARDS_ACCOUNT`, `HOLDER_REWARDS_TOKEN_ACCOUNT` | rewards worker | `<TOKEN_HOLDER_REWARDS_ACCOUNT>` | `<TOKEN_HOLDER_REWARDS_ACCOUNT>` | Current settlement routes holder rewards to this configured wallet/account; automatic distribution remains disabled unless HOLDER_REWARDS_MODE=automatic is separately tested. |
| Burn mechanics | Token burn instruction/account | Intended token burn fee | Token program / mint supply | Authority depends on implementation | N/A | Fee payer SOL | Burns Baby Kenji | none currently | future fee plumbing | N/A | N/A | Current program calculates burn amount but does not invoke SPL burn. Production fee distribution is blocked if burn is required. |
| Player SOL wallet | EOA | Places SOL/token bets and signs frontend txs | Player | Player secret in wallet adapter | User wallet | Yes for wager, rent, fees | Yes for token wagers | wallet adapter | frontend/program | player wallet | player wallet | Never handled by backend. |
| Player token account | ATA | Holds Baby Kenji wager tokens and receives token wins | Player | Player wallet signs transfers | User wallet | SOL for ATA rent if needed | Yes | derived ATA | frontend/program | derived | derived | Mint must equal Baby Kenji mint. |
| MongoDB connection/user | Mongo/DB config | Backend bet mirror, seeds, rewards cycles/payouts | Backend ops | Password/API secret | Secret config | No | No | `MONGODB_URI` | backend/rewards | `mongodb://127.0.0.1:27017/forge_coinflip` | `<MONGODB_URI>` | Use least-privileged user, backups, TLS. |
| RPC provider/API key | RPC config | Backend/frontend Solana access | RPC provider | URL may include secret key | Secret for backend; public for frontend URL | No | No | `RPC_URL`, `VITE_RPC_URL`, `HELIUS_RPC_URL`, `HELIUS_API_KEY` | all | `<RPC_URL>` | `<RPC_URL>` | Frontend RPC is public; backend RPC key should be rate limited. |
| Admin API token | API secret | Protects any admin HTTP surfaces if enabled | Backend ops | Yes | Secret | No | No | `ADMIN_API_TOKEN` | backend/admin | `<ADMIN_API_TOKEN>` | `<ADMIN_API_TOKEN>` | Current rotate route comment mentions it, but code does not enforce bearer auth; treat as blocker before exposing admin routes. |
| Holder rewards authority keypair | EOA/keypair | Sends reward payouts and creates ATAs | Rewards operator | Yes | Hot/warm limited | Yes for fees and ATA creation | Maybe source authority | `HOLDER_REWARDS_AUTHORITY_KEYPAIR_PATH` | rewards worker | `./keys/rewards-authority.json` | `/run/secrets/rewards-authority.json` | Worker currently has primitives only; implement sender before production. |
| Excluded wallets list | Config | Exclude team/treasury/CEX/etc. from rewards | Rewards operator | No | Config | No | No | `HOLDER_REWARDS_EXCLUDED_WALLETS` | rewards worker | `<ADMIN_WALLET>,<TOKEN_TEAM_FEE_ACCOUNT>` | approved list | Must include vaults, fee wallets, burn/system accounts as policy requires. |
