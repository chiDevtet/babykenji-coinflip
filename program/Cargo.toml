[workspace]
members = ["programs/*"]
resolver = "2"

# overflow-checks on as defense-in-depth; the program also uses explicit checked math.
[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
